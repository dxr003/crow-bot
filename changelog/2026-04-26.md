# 2026-04-26 改动日志

## 11:13 — 成交报告卡片增加"出场价 + 真实账单 PNL U"
**起因**：老大要求平仓报告显示实际成交价 + 真实 PNL U 数（不止百分比）

**改 bull_trailing.py**：
- 新增 `_fetch_realized_for_account()` 用 binance `userTrades` API 拉 SELL+LONG 成交
- 新增 `_aggregate_real_exit()` 多账户加权聚合
- close_msg（被币安SL/TP触发）：加"出场: X.XXXX" + "实际: ±X.XX U"
- timeout_msg（24h超时）：加 `time.sleep(1.5)` 等 binance 落库 + "出场" + "实际"
- 拉取失败回退到 mark + 标"拉取失败"

**回滚**：解封 → 还原 helper + 模板 → 重封

## 11:53 — DN 因子加趋势/位置守门（AGT/BSB 双损根因）
**起因**：04-26 上午 AGT(1m+10.2%) -49.28U + BSB(1h-7%, 3m+6.6%) -50.49U 连损 100U
- AGT 死法：1m 已拉到 +10.2% 才进场（FOMO 顶部接盘），34 分钟回吐 -10% 触 SL
- BSB 死法：**1h-7% 跌势中**，3m+6.6% 抢反弹假阳，9 分钟破 SL
- 共同根因：DN 短时爆发因子单独触发（1m 55 / 3m 42 分），评分系统把"刚拉飞"当"机会"

**改 analyzer.py:92-148**：
- 1h 趋势硬否决：`change_1h < trend_reject_1h_pct(默认0)` → DN 全部归零，breakdown 标 "DN.1h趋势否决(-X%)"
- 1h 弱趋势降权：`change_1h < trend_weak_1h_pct(默认5)` → DN 因子 ×0.5
- 1m 山顶守门：`change_1m ≥ peak_1m_pct(默认10)` → DN 因子 ×0.5
- 两个降权可叠加（最低 ×0.25）

**回归验证**（重放当时数据）：
- BSB(1h-7%)：60 → **20 分**（DN 否决，只剩 AF 20）→ 拒
- AGT(1m+10.2%)：73 → **37 分**（DN 砍半 27 + DD 8 + AF 10）→ 拒
- 健康对照(1m+9%, 1h+10%)：75 分（通过，门槛宽松度保留）

**回滚**：解封 analyzer.py → 删 line 96-148 守门段（保留原 dn_reverse） → 重封 + restart bull-sniper

**封板状态**：bull_trailing.py + analyzer.py 已 +i 重封

## 14:00 — 潮汐 funding 阈值漂移修复（crow-review 发现）
**起因**：玄玄 /复查潮汐 → crow-review 扫出 funding_rate 阈值漂移 100 倍
- runner.py:117 写 `0.05`（5% 极端费率，永远不可能触发，破箱 4 因子事实上只有 3 因子有效）
- decision_engine.py:24 写 `0.0005`（0.05%，正确）
- memory `project_tide_2026-04-22.md` 文档写 `0.05`（写错了，意图是 0.05%）

**根因**：binance API 的 `funding_rate` 字段是小数（0.0001 = 0.01%），历史最大极端值约 ±0.5%（即 0.005），永远到不了 5%

**改 runner.py:117**：`abs(funding_rate) >= 0.05 → >= 0.0005`，加注释 "0.05%（binance funding_rate 是小数）"
**改 memory**：阈值表同步改为 `0.0005（0.05%）`

**效果**：FK5 破箱强制平仓的 4 因子守门员从"实际 3 因子"恢复成"4 因子全真"

**封板状态**：runner.py 已 +i 重封，tide.service 已重启 active

## /crow-review 改造（同日）
**改 SKILL.md** 4 处：
1. TRIGGER 段：自然语言"复查 X / 审查 X"恢复触发，但**必须模块名 + 动词同时满足**
2. 别名表新增 3 行：潮汐 / 幻影 / 链上007
3. Phase 1 删"扫最近修改"兜底，禁止全 VPS 扫
4. frontmatter description 同步更新（这是 skill 加载机制实际看的字段）

## 16:10 — 移动止盈 + 多账户底座 5 处优化（crow-review 发现）
**起因**：玄玄 /复查多账户 → /复查移动止盈 两轮审查发现真 Bug 2 + 效率坑 3

### Bug（2 条，必修）
1. **trailing_layered.py:101 _save() 改 atomic_write_json** — mkdir+write 非原子，崩溃留半文件（bull_trailing 已用，这边漏了）
2. **trailing_layered.py:454 reduce_position 失败回滚** — `trigger_count++` 原本在 API 调用前 → 减仓失败也记虚假计数。改成"成功才计数"

### 效率（3 条）
3. **trailing.py + trailing_layered.py check_all 加 ThreadPoolExecutor 并行预拉持仓 + markPrice 单轮缓存** — 串行拉 4 账户 ~20ms → 并行 ~5ms；多账户重币时再省 mark API 调用
4. **executor.py:946 _fanout 加 per_account_timeout=8s** — 单账户卡死不再堵全调度（原来堵 30s）
5. **guardian.py:72 check_accounts 加 per_account_timeout=5s** — 同理，巡检不被一个慢账户拖垮

### 驳回的误判（agent 误标）
- ❌ trailing_layered.py:407 SL 上移不撤旧（这是 04-25 ORCA 事故的故意修复方案，closePosition=true 触发任一全平不会反手，**不能改回先撤后挂**）
- ❌ load_dotenv 重复（模块级加载，只跑一次）
- ❌ bull_trailing.py:533 sleep(1.5)（这是上午为"真实 PNL 报告"故意加的等 binance index 落库时间，删掉就回退功能）

### 测试
全套 smoke：4 文件 AST OK / `trailing.check_all()` + `trailing_layered.check_all()` 空 state 不崩 / `executor.get_all_balances('玄玄')` 4 账户全通

### 封板状态
- trailing_layered.py / trailing.py 已 +i 重封
- executor.py / guardian.py 未封板（如原状）
- maomao / damao / bull-sniper / tide 全部 active

## 16:17 — Cooldown 配置 0 → 真值（DUSDT 32 秒追高根因）
**起因**：DUSDT 16:09 进 0.01662 → 16:12 止盈出 0.01772 → **16:13 立刻又开 0.01874**（高位接盘 +12.7%），评分还从 79 降到 67
**根因**：config.yaml 三个 cooldown 都是 0（v3.4 测试模式留下来的，没改回）
- `cooldown_after_tp_hours: 0 → 1`（止盈追高的根本原因）
- `cooldown_after_sl_hours: 0 → 3`
- `cooldown_after_timeout_hours: 0 → 2`
**生效**：config.yaml mtime 热重载 30 秒内自动生效，不重启
**回滚**：直接改回 0

## /shared/core.py — TG 美化反复消失根因修复
**起因**：老大反馈"复制按钮、加粗、代码块过几天又没了，反复消失"
**根因**：`_md_to_html` line 81-86 旧版只转义 `&`，不处理裸 `<` `>`。LLM 输出 "score<60" / "if x<5" 等触发 TG HTML 解析失败 → 走降级路径剥光所有 HTML 标签 → 美化全消失
**改法**：line 81-86 升级"全字符转义+合法标签保护"
1. 先把 `<b>` `<i>` `<s>` `<u>` `<code>` `<pre>` `<a>` `<tg-spoiler>` 等合法标签抽出占位
2. 全裸字符 `&` `<` `>` 转义
3. 恢复合法标签
**测试**：8 个边界 case 全过（裸字符/嵌套粗体/URL含&/代码块裸字符/幂等性）
**封板状态**：core.py 已 +i 重封，maomao + damao 已重启 active


## 23:37 - 00:53 mode 配置 bug
**起因**：23:37 改 config.yaml mode: auto → off，但**没重启 bull-sniper.service**
**后果**：scanner 进程内存里 mode 仍是 auto。00:40 ZBT score=85 触发自动下单 bn4 多 1083 @ 0.2294
**老大发现**：00:52 看到 bn4 还在买进，紧急要求平仓
**处置**：
- 00:53 平 bn4 LDO（凌晨 mode=off 之前残留单，平 -5.6U）+ ZBT（平 +13.5U）
- 重启 bull-sniper 让 mode=off 真正生效
- **教训**：mode 字段不在 mtime 热重载范围，改 mode 必须重启 service。accounts.enabled 才是热重载
