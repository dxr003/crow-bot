# 2026-04-16 变更日志

## 做多阻击 scanner.py 升级

### 1. 配置热重载
- 新增 `_hot_reload_config()` 函数
- 每循环检查 config.yaml 的 mtime，有变化自动重载
- 改参数不用重启 bull-sniper 了
- 已实测验证，日志输出 `[热重载] config.yaml 已更新，参数已生效`

### 2. 下架币过滤
- 新增 `_refresh_trading_symbols()` 函数
- 从 exchangeInfo 获取 TRADING 状态合约，10分钟缓存
- get_all_tickers() 自动排除 SETTLING/下架币
- 效果：663个合约 → 564个（过滤99个垃圾币，如ALPACA）

### 3. 池退出规则完善
- 新增 `pool_exit_upper_pct: 20` — 池内涨幅>20%退出（不追高）
- 新增 `pool_exit_lower_pct: 5` — 池内涨幅跌回<5%退出（动力不足）
- 替换旧的 `gain_since_entry < -15` 规则
- 4个退出点全部接入 reject_tracker

### 4. reject_tracker 集成
- 新文件 reject_tracker.py：跟踪所有池退出，监控6h峰值
- scanner.py 4个退出点调用 record_exit()
- 主循环每轮调用 update_peaks() 更新峰值价格
- 接入 daily_trade_report.py 日报

### 5. 进池推送恢复
- send_pool_entry() 函数已存在但从未被调用
- 现在新币进池会推送通知

### 6. 状态卡片实时同步
- 状态卡片推送从"扫描前"移到"扫描后"
- 确保卡片反映最新池状态

### 7. 开仓金额提升
- config.yaml: position_usd 50 → 100

## 贝贝快照改进
- status_push.py 新增每账户小计行：`📊 小计: xxxU`

## 文件清单
- 修改：scanner.py, config.yaml, status_push.py（均已封板）
- 新增：reject_tracker.py
- 修改：/root/scripts/daily_trade_report.py（reject报告段）

## Git
- commit: 96a7379 + 23a6bf1
- 已推 GitHub
